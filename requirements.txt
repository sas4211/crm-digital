# CRM Digital — Python dependencies
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────────

# AI / LLM
google-genai>=0.8.0            # Gemini 1.5 Pro + Flash (agent, sentiment, language)
anthropic>=0.40.0              # Claude API (NotebookLM enrichment, Claude fallback)
mcp>=1.26.0                    # Model Context Protocol — exposes agent tools

# NotebookLM
notebooklm-py                  # Google NotebookLM (research, briefs, podcasts, quizzes)

# Web / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Channels
twilio>=9.0.0                  # WhatsApp + SMS
google-api-python-client>=2.0.0  # Gmail API
google-auth>=2.0.0

# Utils
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic[email]>=2.0.0

# ── Dev / Testing ─────────────────────────────────────────────────────────────

pytest>=8.0.0
pytest-asyncio>=0.24.0
